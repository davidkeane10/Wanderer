/**
 * API Keys for external data sources.
 *
 * All client-side keys use the EXPO_PUBLIC_ prefix (visible in the browser bundle).
 *
 * Free sources that need no key:
 *   → Reddit (reddit.ts)
 *   → OpenStreetMap Overpass — trails (alltrails.ts) + abandoned places (abandonedOsm.ts)
 *   → Wikipedia (wikipedia.ts)
 *   → Wikidata SPARQL (wikidata.ts)
 *   → Geocoding via Photon/expo-location (geocode.ts)
 */
export {};
